---
import { getEvents } from "../../../data";
import EventProjector from "../../../components/EventProjector.astro";

export async function getStaticPaths() {
  const events = await getEvents();
  return events.map(({ id }) => ({ params: { slug: id } }));
}
---

<EventProjector />
