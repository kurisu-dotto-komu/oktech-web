---
import { getVenues } from "../../data";
import VenuePage from "../../components/VenuePage.astro";

export async function getStaticPaths() {
  const venues = await getVenues();
  return venues.map(({ id }) => ({ params: { slug: id } }));
}
---

<VenuePage />