---
import EventsFilterLocation from "./EventsFilterLocation.astro";
import EventsFilterTopic from "./EventsFilterTopic.astro";

interface Props {
  selectedCity?: string;
  selectedTopic?: string;
}

const { selectedCity, selectedTopic } = Astro.props;
---

<div class="flex flex-wrap gap-4 justify-center items-center">
  <EventsFilterLocation selectedCity={selectedCity} />
  <EventsFilterTopic selectedTopic={selectedTopic} />
</div>
